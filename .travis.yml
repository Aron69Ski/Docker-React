language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t 196969/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true 196969/docker-react npm run test -- --coverage  